/**
 * ADS DISABLED FOR SECURITY
 * All ad syncing has been temporarily disabled to eliminate malicious script injection vectors.
 */
export const useProfileAds = () => {
  return { forceReloadAds: 0 };
};
