/**
 * ADS DISABLED FOR SECURITY
 * All ad fetching has been temporarily disabled to eliminate malicious script injection vectors.
 */
export const useSimpleAd = (_position: string) => {
  return { content: null, isLoading: false, error: null };
};
