google.com, pub-2831295465597549, DIRECT, f08c47fec0942fa0

# --- Unity LevelPlay / Unity Ads ---
# Ad buyers verify sellers listed here; without these lines most networks
# refuse to bid, so the app gets requests but no ads.
# REPLACE the lines below with the exact lines generated in your
# LevelPlay dashboard: Setup -> App-ads.txt (covers LevelPlay, Unity Ads,
# and every network you have enabled). The Unity line below uses the app's
# Unity game ID; confirm it against your dashboard before publishing.
unity3d.com, 800078728, DIRECT
